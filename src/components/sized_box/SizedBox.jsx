import React from 'react'

const SizedBox = ({ width, height }) => {
    return (
        <div
            style={{
                width: width,
                height: height
            }}>
        </div>
    );
}

export default SizedBox;